<div class="container">
  <div class="row">
    <div class="card col-12 mb-4">
    <div class="card-body">
      <h2 class="card-title">
        マイページ
      </h2>

        <p>アイコン</p>
          <%= image_tag @member.image.url %>

        <p class="card-subtitle mb-2">ニックネーム
          <%= @member.name %>
        </p>

        <p class="card-subtitle mb-2">プロフィール
          <%= @member.profile %>
        </p>

        <hr class="devise-link my-5">
        <% if current_member == @member %>
          <%= link_to "編集", edit_member_registration_path, class: 'btn btn-info btn-block mb-4' %>
        <% else %>
          <%= link_to "トップページに戻る", root_path, class:'btn btn-info btn-block mb-4' %>
        <% end %>
    </div>
    </div>
  </div>
</div>